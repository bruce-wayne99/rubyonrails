<%= form_for subgenre, class: "form-group", url: path do |form| %>
  <% if subgenre.errors.any? %>
    <div  class="alert alert-danger" >
      <h2><%= pluralize(subgenre.errors.count, "error") %> prohibited this subgenre from being saved:</h2>

      <ul>
      <% subgenre.errors.full_messages.each do |message| %>
        <li>
          <%= message %>
        </li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :subgenre_name, class: "form-control", placeholder: "Enter Subgenre Name"  %>
  </div>
<br/>
  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :subgenre_description, class: "form-control", placeholder: "Description" %>
  </div>
<br/>
  <div class="actions">
    <%= form.submit class: "btn btn-default" %>
  </div>
<% end %>
